import React from "react";

const NavbarNutri = ({brand}) => {
    return (
            <nav>
                <div>
                    <a href="#" className="navbar-brand">{brand}</a>
                </div>
            </nav>
    );
}

export default NavbarNutri;